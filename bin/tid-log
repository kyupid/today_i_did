#!/bin/bash
# Called by tmux hook - sends notification on session attach/create

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
source "$SCRIPT_DIR/config.sh"

SESSION_NAME=$(tmux display-message -p '#S')
TIME=$(date '+%H:%M')
DATE=$(date '+%Y-%m-%d')

# Save to local log
mkdir -p "$SCRIPT_DIR/logs"
echo "$TIME - $SESSION_NAME" >> "$SCRIPT_DIR/logs/$DATE.log"

# Send to ntfy (only if topic is configured)
if [[ "$NTFY_TOPIC" != "YOUR_TOPIC_HERE" && "$NTFY_TOPIC" != "your-random-topic-name-here" && -n "$NTFY_TOPIC" ]]; then
    curl -s -d "🔧 $TIME - $SESSION_NAME" "https://ntfy.sh/$NTFY_TOPIC" > /dev/null 2>&1 &
fi
